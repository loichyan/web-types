# == BEGIN TYPE DEFINITIONS == #
# Patterns listed as follows will be replaced:
# - `%s`: the name of the entry
template: <string>

meta:
  link: <template>
  # default: false
  experimental: <boolean>
  # default: false
  deprecated: <boolean>
  # default: false
  nonstrandard: <boolean>

enum_raw:
  # Metadata for this enum.
  $meta: <meta>
  # Default values of enum entries.
  $default: <meta>
  # Enum entries.
  <string>: <meta>
enum: <list<string>>|<enum_raw>
type: <string>|<enum>

attr_raw:
  type: <string>|<list<string>>
  prop: <prop>
  <keyof meta>: <typeof meta>
# `<string>` will be converted to `attr.type`.
attr: <string>|<attr_raw>

prop_raw:
  name: <string>
  # inherit: $default : attr.type
  type: <string>
  # inherit: $default : attr.<meta>
  <keyof meta>: <typeof meta>
# `<string>` will be converted to `prop.name`.
prop: <string>|<prop_raw>

event_raw:
  type: <string>
  <keyof meta>: <typeof meta>
# `<string>` will be converted to `event.type`.
event: <string>|<event_raw>
# == END TYPE DEFINITIONS == #

# Default metadata for each preset entry.
$default: <meta>

# Preset entries.
<string>:
  # The DOM interface of this element preset.
  $type: <string>
  # Metadata for this preset.
  $meta: <meta>
  # Default values of attribute entries.
  $default: <attr>
  # Extend attribute and event entries from other presets.
  $extend: <list<string>>
  # Events.
  $event:
    # Default values of event entries.
    $default: <event>
    # Event entries.
    <string>: <event>
  # Attribute entries.
  <string>: <attr>
